{
  "name": "@dmMiniPlayer/adapter",
  "version": "0.0.1",
  "description": "",
  "type": "module",
  "scripts": {
    "clean": "rm -rf dist",
    "build:dev": "pnpm run clean && sh scripts/build.sh dev"
  },
  "dependencies": {
    "commander": "^14.0.3"
  },
  "devDependencies": {
    "tsx": "catalog:",
    "tsup": "catalog:"
  }
}