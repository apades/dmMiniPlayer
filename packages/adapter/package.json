{
  "name": "@dmMiniPlayer/adapter",
  "version": "0.0.1",
  "description": "",
  "type": "module",
  "scripts": {
    "clean": "rm -rf dist",
    "dev": "pnpm run clean && sh scripts/build.sh dev",
    "build": "pnpm run clean && sh scripts/build.sh"
  },
  "dependencies": {
    "commander": "^14.0.3"
  },
  "devDependencies": {
    "tsx": "catalog:",
    "tsup": "catalog:"
  }
}