import type { PlasmoMessaging } from '@plasmohq/messaging'

const handler: PlasmoMessaging.PortHandler = async (req, res) => {
  // console.log('performance', req.body)
  // res.send(req.body)
}

export default handler
